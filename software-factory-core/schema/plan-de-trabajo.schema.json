{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://software-factory/schema/plan-de-trabajo.schema.json",
  "title": "Plan de Trabajo",
  "description": "Forma canonica del Plan de Trabajo segun T7. El esquema valida forma y tipos. Las restricciones de contenido que cubren las ocho reglas no se declaran aca: si el esquema las rechazara, el verificador devolveria regla 0 y no llegaria a evaluarlas.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "plan_id",
    "run_id",
    "pedido_id",
    "sucede_a",
    "restricciones",
    "unidades",
    "supuestos",
    "fuera_de_alcance"
  ],
  "properties": {
    "plan_id": { "type": "string" },
    "run_id": { "type": "string" },
    "pedido_id": { "type": "string" },
    "sucede_a": { "type": ["string", "null"] },
    "restricciones": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "techo_costo",
        "techo_tiempo_min",
        "techo_iteraciones",
        "alcance_excluido"
      ],
      "properties": {
        "techo_costo": { "type": "number" },
        "techo_tiempo_min": { "type": "number" },
        "techo_iteraciones": { "type": "number" },
        "alcance_excluido": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "unidades": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "enunciado",
          "criterios",
          "dependencias",
          "rastreo",
          "artefacto_esperado",
          "ruta_artefacto"
        ],
        "properties": {
          "id": { "type": "string" },
          "enunciado": { "type": "string" },
          "criterios": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "condicion_observable": { "type": "string" },
                "resultado_esperado": { "type": "string" },
                "procedimiento": { "type": "string" }
              }
            }
          },
          "dependencias": {
            "type": "array",
            "items": { "type": "string" }
          },
          "rastreo": { "type": "string" },
          "artefacto_esperado": { "type": "string" },
          "ruta_artefacto": {
            "type": ["string", "null"],
            "description": "La ruta exacta del artefacto, si el plan decide fijarla. Es obligatorio y admite null: null significa 'no la fijo, la elige el Developer', que es una decision. Omitirlo no seria una decision, seria un olvido, y los dos se leerian igual. C4 exige esta ruta y no lee mas la prosa de artefacto_esperado, donde un ejemplo se volvia obligacion."
          }
        }
      }
    },
    "supuestos": {
      "type": "array",
      "items": { "type": "string" }
    },
    "fuera_de_alcance": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
